@model _3101_proyecto1.FrontEnd.Models.CitumViewModel

@{
    ViewData["Title"] = "Nueva Cita";
}

<h2>Nueva Cita - Paso 1</h2>
@if (ViewBag.mensaje != null)
{
    <h4 class="danger">@ViewBag.mensaje</h4>
}
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create2">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdPaciente" />
            <div class="form-group">
                <label asp-for="HoraInicio" class="control-label"></label>
                <select asp-for="HoraInicio" class="form-control" asp-items="@Model.ListaItems">
                    <option value="">Seleccione la hora</option>
                </select>
                <span asp-validation-for="HoraInicio" class="text-danger" />
            </div>
            <div class="form-group">
                <input type="submit" value="Siguiente" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Cancelar</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}

